# `@bergamota/design-tokens`

Programmatic tokens in `tokens.mjs`. Tailwind v4 preset entry: import `theme.css` before `@import "tailwindcss"` so surfaces, terracotta accent, spacing scale, and type sizes apply globally.
